const express=require('express');const fetch=require('node-fetch');const app=express();const port=process.env.PORT||3000;app.use(express.json());app.post('/',async(req,res)=>{const{url}=req.body;res.status(200).send('ok');if(url){const requests=[];for(let i=0;i<76;i++){const promise=fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})}).catch(()=>{});requests.push(promise);}await Promise.all(requests);}});app.get('/',(req,res)=>{res.status(200).send('ok');});app.listen(port,()=>{console.log(`Service running on port ${port}`);});
