const express=require('express');const fetch=require('node-fetch');const app=express();const port=process.env.PORT||3000;app.use(express.json());app.post('/',async(req,res)=>{const{url}=req.body;res.status(200).send('ok');if(url){let sent=0;while(sent<76){const batch=[];for(let i=0;i<76-sent;i++){batch.push(fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})}).then(()=>true).catch(()=>false));}const results=await Promise.all(batch);sent+=results.filter(r=>r).length;}}});app.get('/',(req,res)=>{res.status(200).send('ok');});app.listen(port,()=>{console.log(`Service running on port ${port}`);});
