const express=require('express');const fetch=require('node-fetch');const app=express();app.use(express.text());app.post('/',(req,res)=>{res.send('ok');const url=JSON.parse(req.body||'{}').url;if(url){for(let i=0;i<76;i++){fetch(url,{method:'POST'}).catch(()=>{});}}});app.listen(process.env.PORT||3000);
