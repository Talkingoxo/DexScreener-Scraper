const express=require('express');const fetch=require('node-fetch');const app=express();app.post('/',(req,res)=>{let data='';req.on('data',chunk=>data+=chunk);req.on('end',()=>{const url=JSON.parse(data||'{}').url;if(url){for(let i=0;i<76;i++){fetch(url,{method:'POST'}).catch(()=>{});}res.send('ok');}else{res.send('no url');}});});app.listen(process.env.PORT||3000);
