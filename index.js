const express=require('express');const fetch=require('node-fetch');const app=express();app.use(express.json());app.post('/',(req,res)=>{const{url}=req.body;if(url){for(let i=0;i<76;i++){fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})}).catch(()=>{});}res.send('ok');}else{res.send('no url');}});app.listen(process.env.PORT||3000);
